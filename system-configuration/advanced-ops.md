### 7. 系统高级运维与用量统计 (Advanced)
* **调整高级选项：** 进入 `高级选项`。
    * **Agent 类型：** 建议保持 `ReAct (推理与执行)`。
    * **Computer 控制：** 开启后，Agent 可以执行自动化桌面操作。
* **远程访问配置：** 开启 `允许外部访问` 并生成 API Key。
* **监控用量：** 进入 `用量统计`。实时查看各模型的 Token 消耗明细和请求记录，精细控制 API 成本。
![高级运维与用量统计示意图](./advanced_ops.png)
